Select [Previous Day to Last Day of the Previous Month] = DateAdd(Day, -2, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [From 30th to 29th] = DateAdd(Day, -3, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [From 29th to 28th] = DateAdd(Day, -4, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [31 Days Back to the Previous Month] = DateAdd(Day, -31, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [32 Days Back to the Previous Month] = DateAdd(Day, -32, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [60 Days Back to the Previous Month] = DateAdd(Day, -60, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [62 Days Back to the Previous Month] = DateAdd(Day, -62, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [63 Days Back to the Previous Month] = DateAdd(Day, -63, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [65 Days Back to the Previous Month] = DateAdd(Day, -65, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [75 Days Back to the Previous Month] = DateAdd(Day, -75, DateFromParts(year(getdate()), month(GetDate()), 1))

Select [75 Days Back to the Previous Month] = DateAdd(Day, -75, DateFromParts(year(getdate()), month(GetDate()), 2))

Select [75 Days Back to the Previous Month] = DateAdd(Day, -75, DateFromParts(year(getdate()), month(GetDate()), 3))